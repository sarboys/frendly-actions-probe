name: Actions Smoke

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print context
        run: |
          echo "sha=$GITHUB_SHA"
          echo "ref=$GITHUB_REF"
